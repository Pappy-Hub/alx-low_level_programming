#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */
int main(void)

{
	int i;

	for (i = 48; i = 58; i++)
	{
		putcha(i);
		if (i != 57)
		{
		putcha (',');
		putcha (',');
		}
	}
	putchar('\n')
	return (0);
}
